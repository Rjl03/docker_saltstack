version: '3'

services: 
  salt-master:
    container_name: salt-master
    hostname: salt-master
    image: docker_saltstack:master
    networks: 
      saltnetwork:
       aliases: 
        - salt-master

  salt-minion1:
    image: docker_saltstack:minion
    container_name: MINION1
    hostname: MINION1
    depends_on:
      - salt-master
    networks: 
      saltnetwork:
       aliases: 
        - MINION1

  salt-minion2:
    image: docker_saltstack:minion
    container_name: MINION2
    hostname: MINION2
    depends_on:
      - salt-master
    networks: 
      saltnetwork:
       aliases: 
        - MINION2

networks:
  saltnetwork:
    driver: bridge
